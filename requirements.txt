Flask==2.2.3                # Flask framework for building the web app
cryptography==39.0.0        # Cryptographic library for encryption and decryption
gunicorn==20.1.0            # Production WSGI server for running the app on Render
itsdangerous==2.1.2         # Required by Flask for secure session handling
Jinja2==3.1.2               # Template engine used by Flask
MarkupSafe==2.1.2           # Required by Jinja2 and Flask
Werkzeug==2.2.3             # WSGI utility library used by Flask
